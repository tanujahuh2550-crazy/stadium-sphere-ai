/**
 * @deprecated Import from `@/services/authService` and use the `useAuth`
 * hook instead. Kept as a thin re-export for backwards compatibility.
 */
export { authService, ROLE_LABEL } from "@/services/authService";
export type { AuthUser, Role } from "@/services/authService";
